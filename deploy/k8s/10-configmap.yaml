# Non-secret runtime configuration shared by the backend pods.
# Secrets (JWT/CSRF/CA passphrase, DB password) live in 11-secret.yaml.
apiVersion: v1
kind: ConfigMap
metadata:
  name: fleet-config
  namespace: fleet-terminal
  labels:
    app.kubernetes.io/name: fleet-terminal
    app.kubernetes.io/component: backend
data:
  FLEET_ENV: "production"
  FLEET_HTTP_ADDR: ":8080"
  FLEET_PUBLIC_URL: "https://fleet.example.com"
  FLEET_COOKIE_SECURE: "true"
  FLEET_LOG_LEVEL: "info"
  FLEET_LOG_FORMAT: "json"
  FLEET_MIGRATE_ON_START: "true"
  FLEET_ALLOW_BOOTSTRAP: "false"
  FLEET_DB_MAX_CONNS: "20"
  FLEET_DB_MIN_CONNS: "2"
  FLEET_REDIS_URL: "redis://fleet-redis:6379/0"
  FLEET_JUMP_HOST: "jumphost:22"
  FLEET_JUMP_USER: "fleet"
  # On-disk paths — all live on the shared RWX volume (see 30-backend.yaml) so every
  # replica reads/writes the same files (recording replay, retention pruning, backups).
  FLEET_RECORDING_DIR: "/var/lib/fleet/recordings"
  FLEET_SCAN_DIR: "/var/lib/fleet/scans"
  FLEET_BACKUP_DIR: "/var/lib/fleet/backups"
  FLEET_UPDATES_DIR: "/var/lib/fleet/updates"
  # Internal sidecar endpoints (see 32-ansible-runner.yaml / 33-grype-scanner.yaml).
  FLEET_ANSIBLE_RUNNER_URL: "http://fleet-ansible-runner:8000"
  FLEET_GRYPE_SCANNER_URL: "http://fleet-grype-scanner:8000"
